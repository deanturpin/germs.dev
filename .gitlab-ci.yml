Build:
  image: ubuntu:devel
  stage: build
  script: cat /etc/os-release

website:
  stage: deploy
  trigger: germs-dev/turpin-one
  only: [main]

cv:
  stage: deploy
  trigger: germs-dev/cv
  only: [main]

